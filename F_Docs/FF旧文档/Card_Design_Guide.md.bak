# 卡牌设计规范

## 1. 文档定位
本文档用于统一维护卡牌相关的通用规范。  
它只负责定义卡牌字段、卡牌类型、稀有度、关键词与文本书写规则，不负责维护具体角色卡组内容。

## 2. 卡牌标准字段
卡牌条目统一包含以下字段：

* 名称：唯一标识这张牌。  
* 所属单位：角色卡默认填写；通用卡静态模板可为空，入组、生成或复制时必须确定这张牌当前按哪个单位结算。  
* 类型：定义这张牌属于哪类卡牌。  
* 费用：定义打出这张牌需要消耗多少资源。  
* 稀有度：定义这张牌的内容级别与稀缺程度。  
* 关键词：记录这张牌自带的规则性质。  
* 文本：记录这张牌的实际牌面效果。  

## 3. 卡牌所属与判定
卡牌所属用于确定这张牌当前按哪个单位结算，并服务哪一类内容结构。

* `角色卡`：所属单位为某个角色，只服务该角色的主轴、机制与成长方向。  
  主要职责是承接角色战斗身份、专属机制与成长路线。角色卡通常构成该角色的基础牌、特有卡和与角色绑定的核心牌池。

* `通用卡`：进入卡组前不预设所属单位，服务通用战斗功能、通用节奏或通用构筑方向。  
  主要职责是补充所有角色都可能使用的基础功能，例如通用输出、通用防御、通用资源修正或通用节奏牌。通用卡不应直接替代角色卡的身份作用。

* `衍生牌`：由卡牌、状态或其他效果生成，不作为普通卡池主体。  
  主要职责是承接战斗中的临时生成、派生效果或特殊系统表现。衍生牌默认不是构筑主轴，而是为当前回合或当前战斗服务的附加内容。

补充规则：
* `所属单位` 字段用于确定这张牌按谁的属性结算、按谁的联动规则判定。  
* 通用卡进入卡组时，必须指定所属单位。  
* 通用卡在被打出时，默认使用其所属单位的属性，并参与该所属单位可用的联动判定。  
* `衍生牌` 是所属与判定概念，不是稀有度概念。  
* 一张牌先有所属单位，再讨论类型、稀有度与关键词。  
* 生成牌默认必须在生成时确定所属单位。  
* 复制牌默认保留原牌所属单位。  

## 4. 卡牌类型
正式卡牌类型统一为：

* `攻击牌`：用于承载直接进攻、伤害与削韧类效果。  
  主要职责是处理敌人血量、推进 Break、完成收割。常见效果包括直接伤害、额外削韧、对 Break 目标追加收益。

* `技能牌`：用于承载防御、治疗、辅助、资源修正与功能性效果。  
  主要职责是稳场、补资源、处理状态、提供功能支援。常见效果包括获得护盾、回复生命、抽牌、返 AP、施加或移除状态。

* `能力牌`：用于承载持续生效的规则效果或整场被动效果。  
  主要职责是改变整场出牌逻辑、强化角色主轴或提供长期被动。能力牌不追求一次性爆发回报，而是通过持续生效体现价值。

补充规则：
* 同一张牌应尽量只归入一种类型，不做复合类型。  
* 若一张牌同时具备伤害与功能效果，应按其主要职责判定类型。  

## 5. 稀有度
卡牌稀有度统一为：

* `普通`：基础构筑内容与最常见的卡牌层级。  
  主要职责是构成牌组的基础骨架，提供稳定、直观、易理解的效果。普通牌优先承担通用功能，不依赖复杂条件链，也不负责定义整套构筑方向。

* `稀有`：比普通牌更强或更有方向性的卡牌层级。  
  主要职责是强化某一类玩法、某一类节奏或某一类角色主轴。稀有牌可以开始体现明显特色，但仍应保持用途清楚，不宜堆叠过多复杂机制。

* `罕见`：更少见、构筑指向更明确的高价值卡牌层级。  
  主要职责是提供更强的成长回报和更清晰的构筑偏向。罕见牌通常可以承担更高强度、更明显联动或更强节奏改变，但仍应保证玩家第一眼能理解其核心价值。

* `传说`：最稀缺、最具代表性或最能改变构筑方向的卡牌层级。  
  主要职责是提供最强的代表性效果、关键构筑转向或高上限收益。传说牌应有明确存在感，但不能只靠数值膨胀，更应体现玩法身份和构筑意义。  

补充规则：
* 稀有度只描述内容分级，不描述卡牌来源。  
* 稀有度越高，不代表文本一定越长；高稀有度牌仍应优先保证可读性。  
* 稀有度越高，不代表可以脱离系统边界单独超模。  
* 角色卡仍应服从角色定位，通用卡仍应服从通用功能边界。  

## 6. 关键词
关键词描述卡牌的通用规则性质。

当前通用关键词口径：

* `快速`：带有该关键词的卡牌在被打出时，不会触发本次先机减少事件。  
* `开战`：带有该关键词的卡牌在进入战斗时置于抽牌堆顶，并在开局抽牌阶段优先被抽入手牌。  
* `唯一`：一般作用于能力牌；带有该关键词的卡牌不可被复制。  
* `保留`：带有该关键词的卡牌在回合结束时，不会因通常弃牌流程离开手牌。  
* `主导`：每回合开始时进入待判定状态；若本回合玩家从手牌主动打出的第一张卡牌带有该关键词，则触发其主导效果。  
* `回收X`：带有该关键词的卡牌从手牌被打出并结算后，会先进入弃牌堆，再立刻回到手牌，并将 `X-1`；当 `X` 降为 `0` 时，清除该关键词。  
* `消耗`：带有该关键词的卡牌在结算后直接进入消耗区，不进入弃牌堆，也不参与普通牌区循环。    

补充规则：
* 关键词只写正式规则词，不写状态名、资源名或设计备注。  
* 没进入统一词表的词，不先写成正式关键词。  

## 7. 文本规范
卡牌文本统一遵守以下要求：

* `短`：文本尽量精简，避免无效描述。  
* `直`：文本直接描述效果，不绕弯解释。  
* `可预判`：文本应让玩家能明确预期这张牌会产生什么结果。  

书写规则：
* 优先使用明确倍率、百分比与阈值表达。  
* 先写主要效果，再写附加效果。  
* 不把状态名和关键词混写。  
* 同一张牌尽量不要承担过多层条件链。  
* 已在 `类型` 或 `关键词` 字段中表达的信息，不在文本中重复书写。  

常用句式：
* 伤害统一写为：`对目标造成相当于攻击力 X% 的伤害。`
* 护盾统一写为：`获得相当于防御力 X% 的护盾。`
* 治疗统一写为：`回复 X% 生命份额血量。`
* AP 回复统一写为：`回复 X AP。`
* 生成统一写为：`生成 1 张某牌到手牌。`
* 打出限制统一优先写为：`若不满足条件，则不能打出此牌。`
* 条件句统一优先写为：`若满足条件，则触发结果。`
